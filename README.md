# Getting started

Clone the application:

    git clone https://github.com/Rubyfuza/aws-workshop.git

Install the dependencies:

    cd aws-workshop
    bundle

Run tests:

    rake

Launch server:

    rails server
